/**
 * Barrel du module file d'attente.
 * `QueueStore` = implémentation locale de démonstration (Observable + localStorage).
 * L'API serveur correspondante est décrite par `QueueService` (core/services).
 */
export { QueueStore } from './QueueStore'
